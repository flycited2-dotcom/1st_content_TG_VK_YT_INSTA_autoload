# Контент-завод (Content Factory)

> Главный репозиторий омниканального проекта Telegram / VK / Instagram / YouTube.
> Текущий рабочий этап — полуавтоматический товарный контент со строгим ревью.
> Общая последовательность развития: [docs/OMNICHANNEL-ROADMAP.md](docs/OMNICHANNEL-ROADMAP.md).
> Согласованный стандарт карточек стабилизаторов: [docs/CARD-STYLE-STABILIZERS.md](docs/CARD-STYLE-STABILIZERS.md).
> Обязательная точность товара и поиск отсутствующих фото: [docs/PRODUCT-IMAGE-FIDELITY.md](docs/PRODUCT-IMAGE-FIDELITY.md).

Автоматический конвейер: **берёт товары из БД → считает цену → пишет краткое описание →
генерирует карточку-картинку (фотоагент) → проверяет правилами → публикует в Telegram-канал**.
Полностью по расписанию/задачам, без ручной возни.

> Это **новый проект**, но не с нуля: «движок» (источник данных, мост к фотоагенту, очередь,
> авто-побудка агента, генерация описаний, режимы карточек, авто-ретрай) **уже построен и обкатан**
> в проекте `avito-bridge` (публикация кондиционеров на Avito). Здесь мы **переиспользуем этот движок**,
> убираем Avito-специфику (XML-фид) и заменяем публикацию на **Telegram-канал**.

## Цель (словами владельца)
Есть «обсервер-агент» (оркестратор), которому ставят задачу: «возьми сегодня 10 карточек из
*<категория>*, сгенерируй краткое описание, отправь в фотоагент, получи карточку, проверь точность,
опубликуй в моём Telegram-канале — завтра в 10:00 и 14:00». Задачи ставятся на день/неделю вперёд,
дальше всё крутится само. Потом владелец добавляет товары/поставщиков/категории — без переписывания.

## Ключевые решения (согласованы с владельцем)
1. **Публикация:** Telegram **Bot API**, бот — администратор канала (`sendPhoto` + подпись). Бот и права уже есть.
2. **Ревизия (точность):** **детерминированные правила, без LLM** (цена>0, ТТХ на месте, карточка
   сгенерирована, описание непустое/без стоп-слов). Не прошло — не публикуем, помечаем.
3. **Постановка задач:** основной путь — **команды боту в Telegram**; плюс поддержать **файлы-планы YAML**
   (оба способа кладут задачи в одну очередь, исполняет планировщик).
4. Контент-движок и описания — **без LLM-ключа** (как в avito-bridge): копирайт детерминированный/шаблонный
   с вариативностью + ручные тексты-переопределения. (LLM можно подключить позже как опцию.)

## Пилот
Первый прогон — на **остатках кондиционеров** (та же БД `oasis`), но публикуем **в Telegram, не в Avito**.
Это даёт рабочий «контент-завод» end-to-end; дальше владелец расширяет категории/поставщиков.

## С чего начать (для агента в свежей сессии)
Читать по порядку:
1. [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — что строим: компоненты, поток данных, Telegram-специфика.
2. [docs/HANDOFF-FROM-AVITO-BRIDGE.md](docs/HANDOFF-FROM-AVITO-BRIDGE.md) — **передача всех наработок**:
   источник данных, фотоагент+очередь+побудка, описания, режимы карточек, сервер/доступы/секреты, грабли,
   карта переиспользования кода.
3. [docs/PHASE-1-PLAN.md](docs/PHASE-1-PLAN.md) — пошаговый план Фазы 1 (TDD), критерии приёмки, открытые вопросы.
4. [CLAUDE.md](CLAUDE.md) — рабочее соглашение по этому репозиторию.
5. Примеры: [examples/config.example.yaml](examples/config.example.yaml),
   [examples/tasks.example.yaml](examples/tasks.example.yaml).

Эталонный код движка лежит рядом: `../Avito/avito-bridge/` (отдельный git-репо). Не править Avito —
**копировать/адаптировать** модули по [REUSE-MAP](docs/HANDOFF-FROM-AVITO-BRIDGE.md#карта-переиспользования-кода).

## Авторизация VK ID

Приложение VK ID пилота: `54732587`. Серверный OAuth 2.1/PKCE хранит короткий
access token и refresh token только в игнорируемом `state/vk-tokens.json`.
Новые пользовательские Access token получают через OAuth VK ID. Контент-завод
запрашивает `vkid.personal_info offline wall photos groups` в защищённом PKCE-потоке.
VK может вернуть только базовые права, если расширенные `wall/photos/groups` ещё не
согласованы для приложения; фактический `scope` обязательно проверяется после обмена.

```powershell
python -m content_factory.publish.vk_oauth start
python -m content_factory.publish.vk_oauth exchange --callback-url "URL_ПОСЛЕ_АВТОРИЗАЦИИ"
python -m content_factory.publish.vk_oauth refresh
```

Для публикации нужен отдельный пользовательский токен Standalone-приложения либо
токен сообщества с подходящими методами. Токены, callback URL с кодом и содержимое
`state/` запрещено добавлять в Git или чат.

Проверка ключа сообщества `241020718` через VK API `5.199` показала: права `wall`
и `photos` у ключа отображаются, но `photos.getWallUploadServer` возвращает ошибку
`27 Group authorization failed`. Загрузка через канал фотографий сообщений также
не подходит: `wall.post` принимает запрос и создаёт запись, но VK молча удаляет такое
вложение со стены. Контрольная запись `wall-241020718_1` поэтому получилась без фото.
Издатель теперь сначала вызывает штатный метод загрузки и при его отказе не переходит к
`wall.post`, поэтому неполный пост не создаётся. Отрицательный `owner_id` не считается
признаком ключа сообщества: пользовательский токен администратора тоже публикует на стену
с отрицательным ID.

Текущий ключ сообщества остаётся рабочим для управления доступными методами сообщества:
через `groups.edit` уже заполнены публичные описание, сайт и телефон. Он также пригоден
для текстовых записей, сообщений, Callback/Long Poll и поддерживаемых разделов. Однако
тип существующего ключа нельзя «доделать» до пользовательского: нативная загрузка фото
на стену требует отдельной пользовательской авторизации через OAuth VK ID с фактически
выданными правами `wall` и `photos`. `dry_run: true` остаётся защитным значением по
умолчанию.

Для закрытого полуавтоматического пилота используется поддерживаемый VK Share:
завод формирует текст, публикует утверждённую карточку по HTTPS и создаёт ссылку
`https://vk.com/share.php`. Владелец вручную подтверждает финальную публикацию.

Целевая площадка пилота — бизнес-сообщество `vk.ru/club241020718`. В конфигурации его
стена записана как `owner_id: -241020718`; отрицательный идентификатор отличает сообщество
от личной страницы. `dry_run: true` остаётся включённым до получения отдельного токена
издателя с правами сообщества и контрольной публикации.

### Почему фото не прикрепляется: измеренная причина (2026-08-27)

Замеры на боевых токенах развели две разные проблемы, которые раньше выглядели одной.

**1. Ключ сообщества не может загружать фото в принципе.** Права у ключа выданы —
`groups.getTokenPermissions` возвращает маску с `photos` и `wall`. Но VK закрывает для
group auth целый класс методов, а не отдельные права:

| Метод | Ключ сообщества |
|---|---|
| `photos.getMessagesUploadServer` | ✅ `upload_url` получен |
| `photos.getWallUploadServer` | ❌ `27` |
| `photos.getUploadServer` (альбом) | ❌ `27` |
| `photos.getMarketUploadServer` | ❌ `27` |
| `photos.getOwnerPhotoUploadServer` | ❌ `27` |

Поэтому настройка прав сообщества этот отказ не лечит: `27 method is unavailable with
group auth` означает «неверный класс авторизации», а не «не хватает права».

**2. Пользовательский токен есть, но с урезанным scope.** VK ID выдал приложению
`54732587` только `vkid.personal_info offline` — без `wall`, `photos`, `groups`.
Запрос в коде корректный (`begin()` шлёт все пять прав, VK ID принимает URL без
`invalid_scope`), значит права режутся настройками приложения. Подставленный живой
пользовательский токен меняет ответ VK с `27` на `15 Access denied: … It cannot be
called with current scopes` — это и есть прямое подтверждение диагноза.

Владелец токена — создатель сообщества, то есть прав внутри сообщества максимум;
не хватает именно согласованного scope у приложения.

**Порядок исправления.**

Права `photos`, `wall`, `groups` для пользовательского токена **не включаются
самостоятельно**. По документации VK в кабинете VK ID настраиваются только данные
профиля: базовые (имя, фамилия, фото, пол, дата рождения, почта) и расширенные
(номер телефона, после подтверждения профиля бизнеса). Остальные расширенные права
выдаются «после индивидуального согласования» по запросу на `devsupport@corp.vk.com`.
Оговорка «если вы уже запрашивали доступы — выданные сохраняются» относится к старым
приложениям; `54732587` создано в 2026 году и под неё не подпадает.

Старые обходные пути закрыты: Implicit Flow и Authorization Code Flow для
пользовательского ключа отключены 25 июня 2024 года.

1. Подтвердить профиль бизнеса в Кабинете VK ID.
2. Написать на `devsupport@corp.vk.com` запрос прав `photos`, `wall`, `groups` для
   приложения `54732587` со сценарием: публикация карточек товаров на стену
   собственного сообщества `241020718`, владелец приложения — создатель сообщества.
3. После согласования пройти авторизацию заново и проверить фактически выданный scope:

```bash
python -m content_factory.publish.vk_oauth start
python -m content_factory.publish.vk_oauth exchange --callback-url "URL_ПОСЛЕ_АВТОРИЗАЦИИ"
python -c "import json;print(json.load(open('state/vk-tokens.json'))['scope'])"
```

4. Убедиться, что проверка готовности стала `ready` — она теперь берёт пользовательский
   токен из хранилища, а не ключ сообщества из `.env`, и называет недостающие права:

```bash
python -m content_factory.publish.vk_capabilities --tokens state/vk-tokens.json
```

Пользовательский токен VK ID живёт час, поэтому он не хранится в `.env`:
`resolve_publisher_token` берёт его из `state/vk-tokens.json` и обновляет по
`refresh_token`, а ключ сообщества из `VK_ACCESS_TOKEN` остаётся резервом для
текстовых записей.

⚠️ **Лимиты VK API с 7 сентября 2026.** На один аккаунт Кабинета для бизнеса VK ID:
10 000 вызовов в месяц без верификации профиля бизнеса и 100 000 000 после неё.
Один только `cf-vk-plan.timer` даёт ~1 440 запусков в месяц (каждые 30 минут), плюс
ежедневные аналитика, витрина и проверка прав. Фактическое число вызовов на цикл не
замерялось — это нужно сделать до 7 сентября, иначе есть риск упереться в 10 000.
Верификация профиля бизнеса снимает вопрос и требуется в любом случае для прав на фото.

### Переходный VK-мост с ручным добавлением фото

Пока VK не выдал пользовательскому приложению право нативно загружать фотографии,
разрешён отдельный ограниченный поток: один уже прошедший Telegram-публикацию климатический
материал превращается в текстовую запись сообщества, а готовую карточку владелец прикрепляет
через редактирование записи. Это не подмена штатного `publish`: мост вызывается отдельной
командой, пропускает `sold`/«ПРОДАНО», проверяет наличие файла карточки и хранит собственный
антидубль в SQLite.

```bash
# безопасный просмотр кандидата, без публикации
python -m content_factory.publish.vk_text_sync

# один реальный пост; второй за этот запуск никогда не отправляется
python -m content_factory.publish.vk_text_sync --publish
```

Прод-таймер `cf-vk-text.timer` запускает мост раз в сутки в 10:15 с небольшим случайным
смещением. Источник `/opt/content-factory/state/content_factory.db` открывается только для
чтения; состояние VK хранится отдельно в `/opt/content-factory-vk/state/vk-sync.db`.

### VK Content Planner v1

Планировщик `content_factory.orchestrator.vk_content_plan` формирует очередь на 14 дней
(понедельник–суббота, один пост в день), чередует категории и бренды и использует только
уже прошедшие Telegram-review материалы с повторно проверенной ценой и наличием. Состояние
хранится в `/opt/content-factory-vk/state/vk-plan.db`.

```bash
# dry-run: сформировать план, но ничего не отправлять
python -m content_factory.orchestrator.vk_content_plan

# L1: отправлять по одному материалу на ревью и после одобрения создавать отложенный текст
VK_PLAN_PUBLISH=1 python -m content_factory.orchestrator.vk_content_plan
```

В Telegram владелец нажимает «Одобрить для VK» или «Пропустить». После одобрения завод
создаёт отложенную текстовую запись и просит вручную прикрепить готовую карточку. За три
часа до выхода неподтверждённого фото приходит повторное предупреждение. Повторные запуски
идемпотентны и не создают второй материал из того же источника. `cf-vk-plan.timer` запускает
цикл каждые 30 минут; без `VK_PLAN_PUBLISH=1` он остаётся безопасным dry-run.

Команда владельца `/vkplan` показывает код материала, название, статус, дату, номер и
прямую ссылку на запись VK, а также следующее требуемое действие. Материал, пропустивший
дату до создания записи VK, автоматически переносится в первый свободный слот и заново
проходит ревью. Если наступило время уже созданной записи, планировщик отдельно фиксирует
подтверждённое фото либо ошибку `photo_overdue`; все исправления сохраняются в
`vk_plan_events`.

Редакционный контур этапа 2 находится в `content_factory.agents.editorial`. Он выбирает
полезную тему, собирает разрешённые факты из `config/vk-editorial-sources.yaml`, формирует
отдельный VK-текст и пропускает его через строгого критика. Технический тезис не может
появиться без связанного официального источника; исследовательский пакет и вердикт
сохраняются в таблице `vk_editorial_audit`. Политика и общественные новости заблокированы.
Пока VK не выдал загрузку фото, редакционные материалы планируются как текстовые записи;
товарные публикации продолжают требовать ручного подтверждения фотографии.

`content_factory.storefront.vk_catalog` готовит управляемую витрину из 15–30 опорных
товаров. В неё проходят только доступные позиции с повторно проверенной ценой, готовой
квадратной первой фотографией и персональной ссылкой заявки в Telegram. Ежедневный
`cf-vk-storefront.timer` обновляет JSON-манифест и помечает исчезнувшие позиции
неактивными. Сам манифест не является обходом VK API и не загружает товары в сообщество:
нативная синхронизация включается после получения права на фотографии.

### Безопасное переключение нативных фотографий VK

`content_factory.publish.vk_capabilities` проверяет фактическую доступность
`photos.getWallUploadServer`, но не загружает файл и не создаёт публикацию. Результат без
секретов записывается в `state/vk-capabilities.json`; `cf-vk-capabilities.timer` повторяет
проверку ежедневно и сообщает в Telegram только при первом переходе в состояние ready.

```bash
# безопасная диагностика, без публикации
python -m content_factory.publish.vk_capabilities

# включать только после успешной контрольной публикации
VK_NATIVE_PHOTO_ENABLED=1 VK_PLAN_PUBLISH=1 \
  python -m content_factory.orchestrator.vk_content_plan
```

По умолчанию `VK_NATIVE_PHOTO_ENABLED=0`. Если флаг включён, а VK отклоняет загрузку,
товар остаётся одобренным в очереди: система не заменяет его текстовым постом и не теряет
фотографию молча.

Редакционные материалы без тематического изображения не публикуются: агент формирует
фотореалистичное задание без текста, логотипов и водяных знаков, а запись получает статус
`visual_pending`. После появления файла
`assets/generated/editorial/<idea-id>.png` планировщик прикрепляет его и только тогда
передаёт материал на редакторское подтверждение. Это правило действует для полезных,
сервисных, сравнительных и доверительных публикаций.

Обычные информационные публикации не содержат внешней ссылки. Ссылка на сайт добавляется
только сервисному материалу при `VK_EDITORIAL_SERVICE_CTA_ENABLED=1` и ведёт через короткий
адрес `/go/svc<id>` на форму `/service/` с UTM-атрибуцией. Форма проверена и работает
(2026-08-28: `splithome.ru/service/` отдаёт 200, короткая ссылка редиректит на неё с UTM),
поэтому флаг включён.

Тематические материалы ведут в каталог по префиксам `/go/ac|hp|rc|vt|st|ups<id>` на
`climat-simf.ru`. Эти редиректы отдаёт приложение сайта, а не nginx; их карта зафиксирована
комментарием в [deploy/nginx/splithome-vk-shortlinks.conf](deploy/nginx/splithome-vk-shortlinks.conf),
а совпадение префиксов с `campaign_short_url` защищено тестом — расхождение уже приводило
к тому, что конфиг знал только устаревший `/go/vkp_<id>`.

### VK-аналитика и автономность

Новые материалы получают уникальный `vkp_<id>`, UTM
`vk / organic_social / content_factory` и, для товаров, отдельную ссылку Telegram-бота.
Клик и итоговая заявка сохраняют один идентификатор публикации. Ежедневный
`cf-vk-analytics.timer` собирает доступные просмотры, охват, реакции, комментарии и
репосты; `cf-vk-weekly-report.timer` по понедельникам отправляет сводку в ревью-чат.

```bash
python -m content_factory.analytics.vk collect
python -m content_factory.analytics.vk report
python -m content_factory.analytics.vk set-level L2
```

Уровень по умолчанию — L1 (ручное подтверждение всего). L2 автоматически одобряет только
полезные, сервисные и доверительные редакционные рубрики; товары и сравнения остаются на
ревью. L3 разрешается кодом лишь после 14 чистых дней. Три ошибочных цикла подряд
принудительно включают L0 и останавливают внешние публикации.

### Дедупликация VK

`content_factory.dedupe` формирует независимую от поставщика модель товара и смысловой
отпечаток публикации. Планировщик хранит общий реестр `vk_dedupe_registry`, журнал
заблокированных повторов `vk_dedupe_events` и одноразовые публикационные блокировки
`vk_publish_claims`. Поэтому изменение цены, ссылки, пунктуации или стандартного CTA не
превращает старый материал в «новый». Витрина использует тот же канонический идентификатор
и оставляет наиболее свежую подтверждённую запись одной модели.
